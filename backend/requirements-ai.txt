# Optional: the AI tree detector used by detector="hybrid" (Auto) on imagery of 0.2 m/px or finer.
# CPU-only torch keeps the install ~1 GB smaller. Without these, Auto falls back to classical and says so.
#   pip install -r requirements-ai.txt --index-url https://download.pytorch.org/whl/cpu --extra-index-url https://pypi.org/simple
# The model weights (weecology/deepforest-tree, ~130 MB) download from Hugging Face on first use.
deepforest==2.1.0
torch==2.14.0
torchvision==0.29.0
